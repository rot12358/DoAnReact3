import orderModel from "../models/orderModel.js";
import userModel from "../models/userModel.js";
import stripe from "stripe"


// place use order for frontend
const placeOrder = async (req,res) =>{

}

export {placeOrder}
